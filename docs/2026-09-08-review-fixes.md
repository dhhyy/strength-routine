# 9/8 · 개발 검토 후 안정화 명세와 검증

기준: `064509f`, 2026-09-08, Asia/Seoul. 사용자 요청은 검토에서 확인한 미흡한 부분의 개발을 계속하는 것이다. [검토 보고서](2026-09-08-development-review.md)의 RV01~04와 관리자 시험 대기 문제를 이번 단위로 처리한다. 이 절은 구현 전에 작성했다. 완료 결과·실행 로그·커밋은 실제 작업 후 아래에 추가한다.

## 구현 범위와 완료 기준

| 단위 | 수정할 원인 | 완료 기준 |
|---|---|---|
| SF01 자동 휴식 재시도 | 파일 저장 실패 후 메모리의 완료 후보를 기존 영구 완료로 오인 | 기본 저장·다음 세트·전용 재시도에서 최초 완료의 자동 시작 의도를 보존한다. 최초 영구 저장 성공 뒤 한 번만 시작하며 기존 완료 수정·제외·초안·취소는 새 자동 시작을 만들지 않는다. 실제 파일 결과와 타이머를 함께 시험한다. |
| SF02 관리자 버전 충돌 | 저장 후 화면의 사용 버전 집합이 과거 상태에 머묾 | 같은 편집 세션의 연속 새 버전 저장 후 이미 사용한 버전을 검토 전에 차단한다. 데이터 불변 보호를 유지하고 버전 오류와 파일 쓰기 재시도의 의미를 구별한다. |
| SF03 시험 대기 | 짧은 고정 실시간 대기 뒤 애니메이션 정착에 의존 | 관리자 이력 복구의 실제 저장 완료를 관찰하며 기다린다. 지연을 명시적으로 주입해 중복/진행 상태와 저장 후 진입을 확인한다. 원래 검증 assertion을 제거하지 않는다. |
| SF04 공용 여백 | 공용 위젯이 기존 토큰값 대신 숫자를 직접 사용 | GlassPanel/해당 HeroCard 여백을 AppSpace로 연결한다. 화면 구성·값·탭은 유지하고 대표 headless 렌더를 확인한다. |
| SF05 현재 문서 계약 | 여러 ‘최신’ 절과 역사 스키마/미구현 목록의 혼재 | 현재 범위·스키마·다음 작업 정본을 명확히 연결한다. 역사 검증 수/커밋을 새 결과로 덮지 않는다. Claude 진입 문서와 실기기 절차도 현행 계약을 가리킨다. |

## 유지할 계약

- 프로그램 원작·활성/보관 계획·실제값·원문 초안·마감 사건·D5 이력을 보존한다.
- 이번 수정에 새 저장 스키마나 새 패키지를 도입하지 않는다.
- 내부 관리자, 사용자 앱, 합성 QA 진입점의 구분을 유지한다.
- 앱의 Console v2 토큰·한국어/숫자 글꼴·5탭과 기존 화면 목적을 유지한다.
- Simulator·실기기를 자동 실행하지 않는다. 검증은 단위/위젯/임시 파일/정적 분석·headless 렌더로 수행한다.
- 원격 운영·결제·계정·실제 문의·OS알림은 이번 오류 수정 범위에 추가하지 않는다.

## 검증 방법

각 기능의 새 실패 재현이 수정 전 실패하고 수정 후 통과하는지 확인한다. 정상 입력뿐 아니라 저장 실패·취소·기존 완료·버전 이력·지연 저장 경계를 함께 검증한다. 필요한 기능별 회귀 후 최종 코드에서 전체 Flutter 시험·정적 분석·문서 로컬 연결·diff를 검사한다. 웹 랜딩과 기본 프로그램 데이터는 이번 수정의 대상이 아니며 해당 코드를 변경하지 않으면 지난 브라우저 결과를 새 실행으로 기록하지 않는다.

원래 [검토 증거](../research/2026-09-08/review/)는 수정 전 상태의 이력으로 유지한다. 이번 증거는 별도 `research/2026-09-08/review-fixes/`에 기록한다. 기능별 코드·시험·명세를 함께 커밋한다.

## 진행·검증 기록

현재는 명세 작성 단계다. 완료 수·시험 결과·커밋을 아직 확정하지 않았다.
