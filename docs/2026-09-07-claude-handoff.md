# 9/7 Claude 인수인계 · 여기서 재개

> 작성일: 2026-09-07 (Asia/Seoul)
> 대상: `apps/strength_routine` · Flutter 네이티브 사용자 앱
> 목적: 대화 이력이 없는 Claude가 확정 사항과 실제 작업 상태를 파악하고 이어서 작업하기 위한 문서

## 먼저 알아야 할 결론

**사용자는 트레이너가 만든 완성된 프로그램을 선택한다. 앱은 운동·세트 구성을 유지하고 중량·시작일·운동요일만 개인화한다.**

첫 마일스톤은 **프로그램 선택·시작 → 운동 기록 → 앱 재실행 후 복원**이다. 9/7 후속 작업에서 프로그램 계약·시작 화면·실제 세트 입력·로컬 저장을 연결했다. **실제 배포 카탈로그는 아직 비어 있다.** 공개 후보 조사만 승인됐고 특정 프로그램 선정·배포는 미확정이다. 트레이너 등록 도구는 사용자가 별도로 나중에 개발한다.

최신 검증 상태: 정적 분석 오류 없음, 전체 39개 테스트 통과. iPhone 17 Pro QA 빌드·설치·Dart VM 실행은 성공했다. Mac 잠금으로 화면 조작·렌더·프로세스 재실행 검증은 아직 수행하지 못했다. 이전의 ‘12개 테스트 / 최근 기록 모델까지만 구현’은 같은 날 앞선 단계의 이력이며 현재 구현 범위를 뜻하지 않는다.

## 읽는 순서와 우선순위

1. 앱의 [CLAUDE.md](../CLAUDE.md)와 워크스페이스의 [AGENTS.md](../../../AGENTS.md): 작업 규칙.
2. 이 문서: 빠른 인수인계. 이후 날짜의 문서가 있다면 그 문서의 변경 사항도 확인한다.
3. [9/7 작업 문서](2026-09-07-work-log.md): **사용자 답변 원문·상태·결정 이유·검증 이력**.
4. [프로젝트 개요](00-overview.md) → [요구사항](01-requirements.md) → [엔진 명세](06-engine-spec.md): 현재 진행 및 상세 요구. 각각의 **9/7 추가 사항부터** 읽는다.
5. UI 작업 시 [디자인 철학](05-design-philosophy.md), [디자인 시스템](03-design-system.md), [공통 방법](../../../design-harness/METHOD.md), 기존 토큰·컴포넌트 확인.
6. [엔진 분석 초안](02-engine-logic.md)은 배경 자료다. 초기 초안의 제안이 최신 사용자 답변을 덮어쓰지 않는다.
7. 개선 기능 개발 시 [운동 앱 비교](2026-09-07-fitness-app-analysis.md) → [기능 상세 분석](2026-09-07-feature-specification.md) → 해당 IM의 하위 문서를 읽는다. 비교 자료의 근거와 상세 문서의 설계 제안을 구분한다.

이 문서는 날짜별 스냅샷이다. **이후 사용자의 명시적 답변과 확정 기록이 우선**한다. 오래된 본문의 미개정 부분, 개발 제안, 사용자 확정을 구분한다.

## 확정된 제품 결정

| 결정 | 근거 |
|---|---|
| 첫 목표는 루틴 시작→기록→재실행 복원을 끝까지 연결 | I01 |
| 최근 기록이 입력된 메인 리프트부터 지원. 기록 없는 시작 중량을 임의로 만들지 않음 | I03 |
| 트레이너가 등록할 대상은 기간·요일·운동·세트가 포함된 완성된 프로그램 | I04·I05 |
| 사용자는 여러 프로그램 중 선택. 모든 프로그램에 하나의 4주 구성을 강제하지 않음 | I04·I05 |
| 프로그램의 운동·세트 구성 유지, 중량·시작일·운동요일 개인화 | I06 |
| 트레이너 등록 도구는 사용자가 별도로 후속 개발. 현재 트레이너 등록 화면·계정·관리 서버를 만들지 않음 | I07 |
| 초기 콘텐츠는 공개 프로그램을 조사해 후보를 제안. 특정 후보의 채택·앱 수록·배포는 아직 미확정 | I08: “공개 프로그램을 조사해 후보를 제안해줘” |
| 9/7부터 날짜별로 인터뷰 답변·결정·작업·검증·다음 재개 지점을 기록 | 사용자 작업 방식 요청 |

확정된 내용을 다시 승인받을 필요는 없다. 구현 이름·파일 배치·테스트 구성 등 일반적인 기술 판단은 개발자가 진행한다.

## 현재 코드 상태

| 파일 / 영역 | 구현된 범위와 한계 |
|---|---|
| [recent_lift_record.dart](../lib/domain/recent_lift_record.dart) | 4종 메인 리프트의 중량·단위·반복·날짜·선택 RIR 검증/JSON 계약. 시작 설정의 최근 기록 입력에 연결 |
| [training_program.dart](../lib/domain/training_program.dart) | 프로그램 ID·버전·주차·세션·운동·세트, 명시적 중량 처방, 시작한 프로그램 전체 스냅샷, 고정된 일정·목표 중량, 수행 기록·초안·메모·완료 알림 상태 |
| [local_training_store.dart](../lib/data/local_training_store.dart), [training_controller.dart](../lib/app/training_controller.dart) | 로컬 JSON 영구 저장·복원. 읽기/쓰기 직렬화, 같은 디렉터리의 임시 파일 write/flush 후 rename, 스키마 검증, 실패 시 원본/입력 보존과 재시도. 카탈로그 공급 경계 분리 |
| [program_screen.dart](../lib/program_screen.dart) | 프로그램 목록·상세·필수 최근 기록·명시적 기준 중량·시작일·요일·중량 간격 설정 → `PlanReviewScreen`의 날짜별 운동·세트·개인화 목표 kg 확인 → ‘이 일정으로 시작’에서 저장 후 활성 루틴으로 이동 |
| [training_screens.dart](../lib/training_screens.dart) | 활성 루틴의 오늘/다음 세션, 실제 저장 데이터 기반 달력·기록 조회, 실제 상태 기반 프로필. 미래 계획 및 이전 계획 기록은 읽기 전용 |
| [workout_screen.dart](../lib/workout_screen.dart) | 세트별 목표/실제 값, kg/lb·반복·RIR·메모 입력, 초안 저장, 완료·제외, 저장 실패 재시도, 필수 세트 완료 조건, 읽기 전용 표시 |
| [main.dart](../lib/main.dart), [onboarding_screen.dart](../lib/onboarding_screen.dart) | 실제 저장소 초기화·복원·실패 UI, 소개 완료 영구 보관, 새 오늘/기록/프로필과 기존 검색/습관을 5탭으로 연결 |
| [tokens.dart](../lib/tokens.dart), [theme.dart](../lib/theme.dart), [fonts](../assets/fonts) | 기존 IBM Plex Sans KR/Mono를 로컬 TTF로 번들. 온라인 폰트 다운로드 의존 제거, OFL 등록·프로필 라이선스 화면 연결 |
| [programs.json](../assets/programs.json) | `schemaVersion: 1`, `programs: []`. 후보를 실제 콘텐츠로 승격하지 않은 빈 배포 카탈로그 |
| [qa_preview.dart](../tool/qa_preview.dart) | QA 전용 진입점. 합성 데이터임을 이름에 표시한 1주·1세션·2세트 수동 처방을 주입하고 `qa-training-state.json`에 별도 저장 |
| [테스트 폴더](../test) | 최근 기록·프로그램/일정/처방·저장소·화면 흐름·세트 입력 테스트. 검증 결과와 네이티브 미확인 범위는 아래 기록 참조 |

새 기록 모델은 스쿼트·벤치프레스·데드리프트·오버헤드프레스 4종을 수용한다. 유효한 중량/반복/RIR 및 주입한 기준일에 따른 미래 날짜 여부를 검사하며, 날짜는 입력된 연·월·일을 UTC 자정으로 보관한다. e1RM·working max 계산은 없다.

**현재는 JSON 변환뿐 아니라 실제 파일 저장소까지 구현했다.** 다만 배포 카탈로그가 비어 있어 일반 실행에서는 빈 상태가 정상이다. 전체 흐름은 QA 전용 프로그램을 주입하여 검증하며, QA 데이터가 출시 콘텐츠에 포함됐다고 해석하지 않는다.

중량 처방은 `manual` / `fixedKg` / `percentOfBaseline` 세 종류다. 비율 처방은 사용자가 명시적으로 입력한 프로그램 기준 중량에 적용하며 최근 반복 기록으로 e1RM을 자동 산출하지 않는다. 일정·계산한 목표 중량은 시작 시 저장하고 복원 때 현재 날짜나 수정된 원본 프로그램으로 다시 계산하지 않는다.

## 후속 조사 · 14개 운동 앱 비교

사용자가 운동 앱을 더 조사하고 그 자료에 근거한 개선 분석을 요청했다. [9/7 종합 분석](2026-09-07-fitness-app-analysis.md)에 14개 앱·공식 본문 40개, [현재 코드 대조](../research/2026-09-07/expanded/current-app-audit.md)의 APP01~15, 개선 백로그 IM01~15와 수용 기준을 정리했다. 개별 출처는 [목록](../research/2026-09-07/expanded/sources.md)을 따른다.

우선 제안은 실제 프로그램 연결·상태 정확성/네이티브 검증 → 운동 ID/수행일 계약 → 지난 기록 재사용·다음 세트·휴식 → 일정·요약·백업이다. **개선안은 분석자의 제안이며 새 사용자 확정 요구가 아니다.** 이 조사에서 앱 코드·저장 데이터·스키마를 변경하거나 새 테스트를 실행하지 않았다. 앞선 구현의 39개 테스트와 미완료 네이티브 QA 상태는 그대로다.

## 후속 요청 · 개선 기능 상세 분석

사용자의 “그리고 문서들을 남기고, 그 기능들을 분석해” 요청에 따라 [기능 분석 정본](2026-09-07-feature-specification.md)과 [프로그램·시작·검색](feature-analysis/2026-09-07-programs.md), [운동 중 기록·요약·정정](feature-analysis/2026-09-07-workout.md), [상태·날짜·일정·복원](feature-analysis/2026-09-07-data-reliability.md)을 추가했다. IM01~15 각각 현재 구현과 차이, 정상 흐름, 논리 데이터, 실패·취소·재실행, 영향 파일과 선행 조건, 수용 기준, 미결정 정책을 기록했다.

개발 전에 특히 다음 경계를 확인한다.

- `isSessionComplete`의 필수 기록 충족, 실제 완료 세트 수, `completionNotified`, 사용자 명시 마감은 서로 다른 의미다. 기존 파일에서 마감 의도를 추정하지 않는다.
- 예정일과 실제 수행일을 분리하고 옛 기록의 실제 날짜는 미상으로 남긴다. 공통 종목 ID를 추가해도 기존 계획·세션·세트 ID를 보존한다.
- 지난 값은 새 초안에만 가져온다. 새 수행 완료의 저장 성공만 타이머 시작 후보이며 정정·취소·재시도·복원은 새 완료 사건이 아니다.
- 설정 초안 취소로 이미 저장한 최근 리프트 기록을 지우지 않는다. 전체 백업 복원은 일반 저장과 달리 기존 쓰기를 정리하고 후보 전체를 검증한 뒤 교체한다.
- 선정 콘텐츠에 필요한 IM14 최소 확장은 IM01보다 먼저 할 수 있다. 모든 편의 기능을 IM06 완료까지 멈출 필요는 없으며 현재 세션 내 다음 세트 이동·집계 문구는 먼저 가능하다.

상세 문서의 필드명·전이·수용 기준은 **개발 제안**이다. 이번 문서 작성으로 코드·저장 스키마를 변경하거나 기능 테스트를 실행하지 않았다. 이후 실제 구현 결과와 검증은 해당 IM 및 날짜별 작업 기록에 별도로 남긴다.

## 아직 답이 없는 항목

1. **첫 공개 후보 선정:** I08의 조사 방식은 답변 완료. [공개 후보 비교](../research/2026-09-07/program-candidates.md)의 [Stronglifts 5×5](https://stronglifts.com/stronglifts-5x5/workout-program/), [GZCLP](https://swoleateveryheight.blogspot.com/2016/02/gzcl-applications-adaptations.html), [5/3/1 for Beginners](https://www.jimwendler.com/blogs/jimwendler-com/5-3-1-for-beginners) 중 어떤 버전·기간을 검토할지 또는 기간형 후보를 추가 조사할지 정한다. 앱 수록/재배포 권한은 세 후보 모두 미확인이다.
2. **실제 프로그램 처방 계약:** 현재 고정 kg·수동·명시적 기준 대비 %를 지원하도록 기술 계약을 구현했다. 특정 후보의 시작 중량·TM·e1RM 공식이 사용자 확정됐다는 뜻은 아니다.
3. **부족한 기록·운동별 처방:** 현재 시작 화면은 프로그램에 필요한 메인 리프트 기록과 비율 처방의 기준 중량을 요청한다. 보조 운동은 `manual`로 목표 중량을 비워 둘 수 있다. 실제 수록 프로그램에 이 계약이 충분한지는 선정 후 검토한다.
4. **I02 — 목표 분류:** 스트렝스·근비대·파워빌딩 3종은 기존 요구사항. 이번 인터뷰는 미응답이며, 프로그램의 분류 항목으로 다룰지도 검토한다.
5. **엔진 D3~D7:** 수치·공식 미확정. D3~D6의 운동·세트/블록 자동 변경은 I06과 적용 범위를 재조정해야 한다. 훈련 후 자동 조절은 첫 생성 흐름과 구분해 후속 인터뷰한다.

기존 D1(hard set)·D2(Lv1/순수 함수)·D8(충돌 우선순위)은 과거 확정 기록이지만, 이것만으로 트레이너 프로그램을 자동으로 재구성해도 된다는 뜻은 아니다.

## 재개할 작업

1. 루트 에이전트가 남긴 최종 검증 기록을 확인한다. 미완료된 테스트/시뮬레이터 QA가 있으면 그 지점부터 이어간다.
2. 공개 후보 비교를 사용자에게 제시하고 **첫 수록 후보·구체 버전·기간**을 정한다. 원작과 다르게 임의로 4주 패키지화하지 않는다.
3. 해당 콘텐츠의 수록 조건과 처방 형식을 확인한 뒤 카탈로그에 연결한다. 초기 계약은 **매주 동일 세션 수·유한 기간·고정 반복**이며 AMRAP, 진급 단계, 무한 주기는 미지원이다.
4. 시작 전 최종 결과 확인 화면까지 구현됐다. 날짜별 운동·세트·목표 중량을 검토한 뒤 확정 저장하는 흐름을 QA한다. **시작 후 일정 변경 UI**는 미구현이다. 미래 일정 변경의 도메인 메서드는 있어도 사용자 UI 완료로 보지 않는다.
5. 검색·습관 목업, e1RM 추세, 자동 조절, 트레이너 도구, 계정·클라우드 동기화·구독은 후속 작업이다. 핵심 흐름 완료와 출시 전체 완료를 구분한다.

화면은 기존 **Console v2**, 한글 `kr()`/라틴·숫자 `mono()`, 앱 토큰·컴포넌트, 하단 5탭을 유지한다. 미검증 수치나 가상 코치·프로그램을 실제 정보처럼 표시하지 않는다.

## 검증과 실행 환경

- 실제 확인한 개발 호스트: **macOS / zsh**. Windows·WSL·컨테이너에서 실행했다고 가정하지 않는다.
- **초기 단계 이력:** 최근 기록 모델 구현 직후 `flutter analyze && flutter test`에서 정적 분석 문제 없음, **12개 테스트 통과**. 당시 UI 변경·새 렌더 검증은 없었다.
- **후속 구현 검증:** 정적 분석 오류 없음, 전체 39개 테스트 통과. iPhone 17 Pro QA 빌드·설치·Dart VM 실행은 성공했다. Mac 잠금으로 화면 조작·렌더·프로세스 재실행 검증은 아직 수행하지 못했다. 파일 복원은 새 저장소/컨트롤러를 만드는 테스트에서 확인했다. 네이티브 재실행 확인과 구분한다. 저장 실패 후 뒤로가기→같은 계획 재시도 회귀를 추가한 `program_flow_test.dart` 2개도 별도로 통과했다.
- Mac 잠금 해제 후 [QA 실행 순서](../tool/README.md)로 렌더·실제 입력·종료 후 재실행을 검증하고, 가장 큰 화면 문제를 수정한 뒤 다시 확인한다. 현재 QA 진입점으로 앱이 실행되어 있다.
- 이 문서 갱신 담당은 코드와 문서를 대조하고 링크를 검토했다. 테스트 실행·시뮬레이터 통과를 문서 담당이 별도로 주장하지 않는다.

**[macOS zsh · 앱 폴더]**

```sh
cd /Users/yudongheon/Documents/vibe-design/apps/strength_routine
git status --short
flutter analyze
flutter test
open -a Simulator
flutter devices
```

실행은 `flutter devices`에 표시된 iPhone 시뮬레이터 ID로 `flutter run -d DEVICE_ID`를 사용한다. `DEVICE_ID`는 실제 값으로 대체한다. 사용자는 Chrome 대신 시뮬레이터 실행을 요청했다.

앱에는 별도 `.git`이 있다. 작업은 앱 저장소 기준으로 확인한다. 이전 작업의 미커밋 변경 및 추적되지 않은 문서·코드가 있으므로 현재 상태를 먼저 살피고 보존한다. 이 작업에서는 커밋하지 않았다.

## 기록을 이어가는 방법

작업일의 `docs/YYYY-MM-DD-work-log.md`에 답변·결정·코드 변경·실행한 검증·남은 항목을 기록한다. 같은 날이면 기존 날짜 문서에 이어 쓴다. 확정 요구는 `01-requirements.md`, 엔진 정책은 `06-engine-spec.md`, 진행은 `00-overview.md`에도 갱신한다.

다음 인수인계가 필요할 때 새 날짜의 문서를 추가하고 `CLAUDE.md`와 개요의 읽기 링크를 최신화한다. 이 문서의 미응답을 추측으로 확정 처리하지 않는다.
