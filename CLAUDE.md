# strength_routine — 작업 규칙 (자동 로드)

## 최신 재개 지점 · 로컬 완성 여섯 기능

최신 최종 검증은 **349개 통과·정적 분석 정상·iOS device 빌드 성공**이다. 실제 기기/Simulator 실행은 하지 않았다.

먼저 [로컬 기능 전체·검증](docs/2026-09-08-local-completion.md), [일정/이전 기록](docs/2026-09-08-schedule-previous.md), [관리자 확장](docs/2026-09-08-local-admin.md), [휴식/가이드](docs/2026-09-08-rest-guides.md)를 읽는다. 아래 고급처방252개·수동휴식만 가능·백업/일정/복사/가이드 없음은 과거 단계의 상태다. 최신 결과·커밋·한계는 상단 문서가 우선한다.

- 카탈로그1~3, 관리자1~4, 운동1~6 읽기; 운동 기본3/고급4/추가처방5, 복원 뒤6+restorationGeneration. 기본5개 asset 변경 없음.
- ProgramSet.repetitionsMax/kind(work,warmup,drop); AMRAP과범위 동시불가. 범위 내 실제반복만 D5비교, 워밍업/드롭은 D5근거/대상 제외. 역사스냅샷 불변.
- 관리자 일괄수정은 exercise/session/week/program 범위와 한필드 선택→후보 검토→적용·undo. 보관은 공개JSON목록 제외, 복원은재포함. 과거 버전은 새버전 초안으로 검토·저장.
- 백업은 운동만 포함, 교체 전 검사/revision잠금/복구본/파일저장성공 후공개. 설정/습관/관리자/휴식은 제외. 손상기록은 원본바이트 보존 후 외부백업복구. 복원세대는 구타이머sourceStamp를 무효화.
- 일정은 오늘/과거/기록있는세션 보호. 이전복사는 같은프로그램ID·버전·내용/종목ID/세트ID·위치·종류로만 연결하며 kg/lb를 그대로 초안복사한다. RIR/메모/수행일/원본기록 불변.
- 설정2(defaultRestSeconds,autoStartRestTimer); 타이머1/2(처방/사용자출처). 새완료 저장성공에만 자동시작, 기존타이머교체확인, 처방0초우선. 모든운동 route에 SettingsScope를 전달한다.
- 기본24종목+9용어 로컬가이드, 명시별칭만연결, 외부출처는사용자선택시 url_launcher. file_picker 파일선택/저장과 함께 실제OS검증은별도.
- 다음 단계는 [실제기기 LC-D01~14](docs/2026-09-08-device-test-guide.md)와 [운영분리 상태표](docs/2026-09-08-remaining-work.md). 시뮬레이터 금지·상세문서·기능별커밋 지시를 유지한다.

## 이전 작업 당시 인수인계


## 이어서 작업할 때 먼저 읽기

최신 전체 검증은 고급 처방까지 **252개 통과·정적 분석 정상**이다. 수행 범위와 한계는 바로 아래 고급 처방 문서 §12가 정본이다.

- **최신 고급 처방 작업:** [9/8 휴식·템포·슈퍼세트·AMRAP 명세와 검증](docs/2026-09-08-advanced-prescriptions.md)을 먼저 읽는다. 관리자 세트별 처방을 사용자 프로그램 상세·시작 전 확인·검색·운동 기록까지 전달하며, 슈퍼세트 다음 이동은 라운드 순서를 따른다. AMRAP 실제 기록은 통계에 포함하되 고정 반복을 전제로 하는 D5 비교 근거·미래 감량 대상에서 제외한다.
- 카탈로그는 **v1/v2**, 관리자 작업 파일은 **v1/v2/v3**, 운동 파일은 **v1/v2/v3/v4**를 읽는다. 고급 처방이 있으면 각각 **v2/v3/v4**, 없으면 기존 **v1/v2/v3**로 쓴다. 기존 5개 프로그램 파일은 변경하지 않는다. 타이머는 `${trainingFile.path}.rest-timer.json`의 별도 v1 파일이며 처방 양수·저장된 완료 세트에서 명시 시작한다. 한 개의 타이머·일시정지/재개/교체 확인·쓰기 실패 재시도·UTC 종료시각 복원을 지원한다. 자동 시작·임의 시간 입력·OS 알림은 없다.
- **최신 관리자 작업:** [9/8 정밀 편집 명세·검증](docs/2026-09-08-admin-precise-editor.md). 프로그램을 원형대로 불러와 주차→세션→운동→개별 세트로 편집한다. 기존 프로그램 복제, 주차 복사·순서·삭제, 50단계 실행 취소, 버전 검토·확정 저장을 연결했다. 원문 초안 자동 저장과 카탈로그 확정 저장을 구분하며 `_committing` 동안 늦은 기본 입력이 구카탈로그를 다시 쓰지 못하게 한다.
- 기존 미완성 기본 초안을 자동으로 정밀 초안으로 변환하지 않는다. 세트 `load.lift`와 운동 `mainLift`는 독립적이다. 웹 관리자 로그인·원격 게시·반복 범위·워밍업 유형·대량 편집·자동 진급은 후속이다.

- **최신 앱 작업:** [수행일·기록 마감 검증](docs/2026-09-08-session-lifecycle-verification.md) → [상세 명세](docs/2026-09-08-session-lifecycle.md) → [후속 10개 개발 로드맵](docs/2026-09-08-development-roadmap.md). 수행일·입력/수정 시각·명시 마감/재개·요약·수행일 달력을 추가했다. 구버전 날짜/마감을 추정하지 않으며 초안을 열고 닫기만 하면 원문을 보존한다.
- **남은 작업·재개 우선순위:** [9/8 상태표](docs/2026-09-08-remaining-work.md). 다음 제안은 운동 기록 파일 백업·복원이고, 일정 변경·지난 값 복사·타이머 자동 시작/OS 알림은 후속이다. 구독/문의 실제 연결·실기기 미검증을 구별한다. 사용자는 랜딩 시안을 집에서 나중에 확인할 예정이다.

- **최신 웹 디자인 작업:** [9/8 인터랙티브 랜딩 5개 시안](docs/2026-09-08-interactive-landing-concepts.md) → [실행·검증 안내](landing/concepts/README.md). `landing/concepts/`에 로컬 Three.js 0.185.1로 중량·프로그램·요일·세트·감량 체험을 구현했다. 02·03은 실제 카탈로그를 읽고, 04·05는 명시적 체험이다. 실제 앱 연결·외부 배포·시뮬레이터 실행은 하지 않았다.

- **9/8 후속 앱 작업이 최신이다:** [기능 검증](docs/2026-09-08-feature-verification.md) → [실기기 확인 순서](docs/2026-09-08-device-test-guide.md). 일반 카탈로그 5개, 내부 관리자 생성, D5/되돌리기, 설정·도움말·구독 준비 안내를 구현했다. 결제·서버 인증·실제 기기 OS 재실행은 구현/검증 경계를 구분한다. 사용자 지시로 트레이너는 운영자 1명이며 다중 트레이너 등록 도구는 제외한다.
- 일반 앱은 `lib/main.dart`, 관리자 내부 도구는 `lib/main_admin.dart`, 실기기 합성 QA는 `tool/device_qa.dart`이다. 관리자/QA를 사용자 배포물로 빌드하지 않는다. 관리자 접근은 계정 인증 대신 내부 빌드 배포 통제를 가정했다.
- 운동 저장의 nullable 수행 메타데이터 JSON 키 생략, 사건 순서/요약 검증, `legacySessionIds`를 보존한다. 프로그램 카탈로그·설정·습관·타이머는 별도 스키마다. 구버전 앱이 고급 처방을 누락한 채 덮어쓰지 않도록 미지원 버전 거부 계약을 유지한다.

- 9/8 웹 작업: [인터랙티브 랜딩 개발·검증·인수인계](docs/2026-09-08-work-log.md) → [landing 실행 안내](landing/README.md). `landing/`은 앱과 별개의 합성 브라우저 체험이다. 앱 카탈로그 5개는 후속 작업에서 등록했고 앱 배포는 준비 중이다.

- 최신 사용자 지시: **시뮬레이터를 띄우지 않고 자체 검증 후 상세 문서화**. 기본 검증은 `flutter analyze`와 `flutter test`이며 사용자 재요청 전까지 시뮬레이터 실행을 자동 재개하지 않는다.
- 최신 검증 결과는 위 고급 처방 명세의 최종 검증 절을 우선하고, [9/7 자체 검증](docs/2026-09-07-verification.md)은 당시 19개 기능의 근거/한계 이력으로 읽는다.
- [9/7 Claude 인수인계](docs/2026-09-07-claude-handoff.md) → [9/7 작업·인터뷰 기록](docs/2026-09-07-work-log.md) → [프로젝트 개요](docs/00-overview.md).
- 기능 구현 이력은 [9/7 기능 개발·커밋](docs/2026-09-07-development.md): 기능 단위 커밋, 검색/습관과 다음 세트·보관 초안을 확인한다. 당시 65개 테스트 이후의 날짜 수정·최종 검증은 위 자체 검증 문서가 우선한다.
- 이후 날짜의 작업 문서가 있으면 함께 확인한다. 최신 사용자 확정 사항이 초기 초안보다 우선한다.
- 9/7 핵심: 트레이너의 완성 프로그램을 사용자가 선택하고 **운동·세트 구성은 유지**, **중량·시작일·요일만 개인화**한다. 트레이너 등록 도구는 사용자 별도 후속 개발이다.
- 날짜별로 인터뷰·결정·작업·검증·재개 지점을 남긴다. 미응답 질문과 제안 수치를 임의로 확정하지 않는다.

**UI/디자인 작업을 시작하기 전에 반드시 `design` 스킬을 먼저 사용한다.**
벗어나야 할 이유가 있으면 먼저 사용자에게 확인한다.

## 정본(SSOT) — 항상 일치시킨다
- **왜(불변)**: `docs/05-design-philosophy.md`
- **어떻게(토큰/가변)**: `docs/03-design-system.md` (요구사항 `docs/01-requirements.md`, 엔진 명세 `docs/06-engine-spec.md`, 배경 초안 `docs/02-engine-logic.md`)
- **코드**: `lib/tokens.dart`·`theme.dart`·`widgets.dart`
- 코드 토큰/위젯을 바꾸면 `docs/03-design-system.md`도 갱신한다.

## 방향 & 스타일
**Direction C · Console v2** — 블루프린트 다크 + 시안, **그라디언트·글래스·은은한 깊이·낮은 밀도**. 모든 시각 결정은 이 컨셉과 `05` 철학에서 파생한다. (단일 다크 테마)

## 불변 vs 가변
- **스타일은 `tokens.dart`에서만** 바뀐다. 스타일 변경 = 새 토큰 세트만. 컴포넌트·레이아웃·프로세스는 그대로.

## 절대 규칙 3가지
1. **토큰 밖 금지.** 색·간격·폰트를 새로 만들지 말고 `AppColors`·`AppSpace`·`AppRadius`·`AppGradients`·`AppShadow`·`mono()`/`kr()`에서 가져온다. (`Color(0x…)`·매직 간격 하드코딩 금지 — `tokens.dart` 예외)
2. **조립 우선.** 새 UI는 먼저 `widgets.dart`의 기존 컴포넌트로 조립. 없을 때만 새 컴포넌트를 만들고 `widgets.dart` + `03 §컴포넌트`에 등록.
3. **새 화면은 `design` 스킬 절차 + `03 §체크리스트`를 통과시킨다.**

## 자주 틀리는 함정
- **한글은 `kr()`, 숫자/라틴은 `mono()`** (IBM Plex Mono엔 한글 글리프 없음 → 깨짐).
- 숫자가 세로로 정렬되는 곳은 `mono`(tabular).
- **깊이는 그라디언트+소프트 섀도우+글래스로.** 카드/보더/그림자·큰 radius 남발 금지.
- **하단 탭 5개 고정**(오늘·기록·검색·습관·프로필). 6번째 금지.
- **의미색**(good/warn/danger)은 상태 전용, 브랜드 액센트와 섞지 않는다.

## 아키텍처
- **엔진(순수 함수) ↔ UI 분리.** 위젯은 엔진이 준 값을 렌더만. 로직 규칙은 격리 단위로 추가(`02 §5`).

## 검증
- 변경 후 `flutter analyze` 통과. UI 변경은 렌더로 계층·대비·정렬·밀도 확인.

## 진행 관리
- 마일스톤(문서·기능 완료) 시 `docs/00-overview.md`의 진행 상태를 갱신한다.
