# strength_routine — 작업 규칙 (자동 로드)

## 현재 재개 지점

2026-09-08. 로컬 기능 완료 기준 `5eaaed8`, 문서별 검토 기준 `064509f`. 사용자 요청에 따라 자동 휴식 재시도·관리자 버전 충돌·관리자 시험 대기·공용 여백 토큰·현재 문서를 보강했다. [검토 후 안정화 명세·검증](docs/2026-09-08-review-fixes.md)의 최종 결과와 커밋을 먼저 확인한다. 과거349개 통과를 새 수정의 검증 결과로 사용하지 않는다.

1. [현재 구현·남은 작업 정본](docs/2026-09-08-remaining-work.md) → [안정화 명세·결과](docs/2026-09-08-review-fixes.md).
2. [여섯 로컬 기능](docs/2026-09-08-local-completion.md) → [일정·이전 값](docs/2026-09-08-schedule-previous.md) → [관리자 확장](docs/2026-09-08-local-admin.md) → [휴식·가이드](docs/2026-09-08-rest-guides.md).
3. [요구사항](docs/01-requirements.md) → [엔진 공개 계약](docs/2026-09-16-engine-contract.md) → [엔진 명세](docs/06-engine-spec.md) → 변경할 기능 명세와 실제 코드.
4. [기기 확인 자료·절차](docs/2026-09-08-device-test-guide.md). 실제 OS/파일 공급자/서명/접근성/결제는 자동 시험과 구분한다.

## 유지할 제품·데이터 계약

- 트레이너 1명의 완성 프로그램을 사용자가 선택하고 운동·세트 구조는 보존하며 중량·시작일·요일만 개인화한다. 다중 트레이너 등록·승인·정산은 제외한다.
- 일반 앱 `lib/main.dart`, 내부 관리자 `lib/main_admin.dart`, 합성 QA `tool/device_qa.dart`를 구분한다. 같은 네이티브 프로젝트의 다른 진입점이며 서버 권한 인증이나 별도 앱 식별자가 아니다. 관리자/QA 실행물을 사용자 배포물로 사용하지 않는다.
- 현행 스키마 전체 표는 [현재 상태표](docs/2026-09-08-remaining-work.md#현재-저장-계약)를 따른다. 카탈로그1~3·관리자1~4·운동1~6을 읽고 내용/복원 세대에 따라 조건부로 쓴다. 설정은1/2 읽기·2 쓰기, 휴식은 처방 출처1/사용자 출처2다. 미지원 포맷을 원본 삭제나 버전 숫자 변경으로 해결하지 않는다.
- 반복 범위와 AMRAP은 동시에 지정하지 않는다. D5는 계획일순을 유지하고 필수 세션 완료를 확인한 뒤 해당 운동의 필수·작업·비AMRAP 세트를 비교한다. 고정 반복은 일치, 범위는 하한~상한 내 실제 반복을 요구한다. 워밍업/드롭/AMRAP은 감량 근거와 대상에서 제외한다.
- 초안 원문, 기존 계획의 처방 스냅샷·목표·실제·수행일 미상·마감 사건·D5 이력을 보존한다. 원래 프로그램 5개를 편집기 변경만으로 소급 수정하지 않는다.
- 관리자 일괄 수정은 네 범위와 여섯 필드 중 하나를 선택해 검토·적용·undo한다. 보관은 다음 JSON 배포 목록에서 제외하고 과거 버전은 새 미사용 버전 초안으로 복구한다. 불변 버전 이력과 편집기 undo는 별개다.
- 운동 백업은 설정/습관/관리자/타이머를 제외한다. 검토·revision 잠금·복구본 확보·파일 저장 성공 뒤 교체하며 손상 원본 바이트도 보존한다. 복원 세대는 옛 타이머 출처를 무효화한다.
- 일정은 오늘/과거/기록 있는 세션을 보호한다. 이전 실제값 복사는 프로그램 ID·버전·구성/종목/세트의 출처를 확인하고 원래 중량·단위·반복만 초안으로 가져온다. RIR·메모·수행일·원본 실제값은 바꾸지 않는다.
- 휴식은 처방0초 우선, 미지정일 때 사용자 기본 시간을 사용한다. 자동 시작은 최초 완료의 영구 저장 성공 뒤 한 번만 하는 계약이며 검토에서 발견한 재시도 오류의 수정 상태는 안정화 문서를 따른다. 기존 타이머 교체 확인과 기록/타이머 저장 실패를 구분한다.
- 기본24종목·9용어 가이드는 오프라인 본문/명시 별칭을 사용하고 외부 출처는 사용자 선택으로 연다. 원격 콘텐츠·계정·결제·문의·OS 알림은 별도 범위다.

## 검증·인수인계 규칙

사용자는 **Simulator를 띄우지 않고 자체 검증·상세 문서화·기능 단위 커밋**을 요청했다. 사용자 재요청 전 Simulator 실행·조작·장애 재시도를 자동 재개하지 않는다. 로컬 임시 파일·단위/위젯 시험·headless 렌더·정적 분석을 기본으로 한다.

[문서별 검토](docs/2026-09-08-development-review.md)는 수정 전 결함과 증거다. [수행일153개](docs/2026-09-08-session-lifecycle-verification.md), [정밀 편집189개](docs/2026-09-08-admin-precise-editor.md), [고급 처방252개](docs/2026-09-08-advanced-prescriptions.md), [로컬 완성349개](docs/2026-09-08-local-completion.md)는 각 단계의 실행 이력이며 서로 합산하지 않는다. 현재 수정 결과는 별도 기록한다.

랜딩은 [다섯 시안 명세](docs/2026-09-08-interactive-landing-concepts.md)와 [실행 안내](landing/concepts/README.md)를 따른다. 사용자는 집에서 시안을 나중에 확인할 예정이다. 로컬 주소를 외부 공유 주소로 안내하지 않는다.

[9/7 인터뷰 기록](docs/2026-09-07-work-log.md)과 [당시 인수인계](docs/2026-09-07-claude-handoff.md)는 결정 이력이다. 이후 사용자 확정과 현행 기능 계약이 우선하며 미응답 제안이나 과거 미구현 목록을 현재 승인/상태로 바꾸지 않는다.

**UI/디자인 작업을 시작하기 전에 반드시 `design` 스킬을 먼저 사용한다.**
벗어나야 할 이유가 있으면 먼저 사용자에게 확인한다.

## 정본(SSOT) — 항상 일치시킨다
- **왜(불변)**: `docs/05-design-philosophy.md`
- **어떻게(토큰/가변)**: `docs/03-design-system.md` (요구사항 `docs/01-requirements.md`, 엔진 명세 `docs/06-engine-spec.md`, 배경 초안 `docs/02-engine-logic.md`)
- **코드**: `lib/tokens.dart`·`theme.dart`·`widgets.dart`
- 코드 토큰/위젯을 바꾸면 `docs/03-design-system.md`도 갱신한다.

## 방향 & 스타일
**Direction C · Console v2** — 블루프린트 다크 + 시안, **그라디언트·글래스·은은한 깊이·낮은 밀도**. 모든 시각 결정은 이 컨셉과 `05` 철학에서 파생한다. (단일 다크 테마)

## 불변 vs 가변
- **스타일은 `tokens.dart`에서만** 바뀐다. 스타일 변경 = 새 토큰 세트만. 컴포넌트·레이아웃·프로세스는 그대로.

## 절대 규칙 3가지
1. **토큰 밖 금지.** 색·간격·폰트를 새로 만들지 말고 `AppColors`·`AppSpace`·`AppRadius`·`AppGradients`·`AppShadow`·`mono()`/`kr()`에서 가져온다. (`Color(0x…)`·매직 간격 하드코딩 금지 — `tokens.dart` 예외)
2. **조립 우선.** 새 UI는 먼저 `widgets.dart`의 기존 컴포넌트로 조립. 없을 때만 새 컴포넌트를 만들고 `widgets.dart` + `03 §컴포넌트`에 등록.
3. **새 화면은 `design` 스킬 절차 + `03 §체크리스트`를 통과시킨다.**

## 자주 틀리는 함정
- **한글은 `kr()`, 숫자/라틴은 `mono()`** (IBM Plex Mono엔 한글 글리프 없음 → 깨짐).
- 숫자가 세로로 정렬되는 곳은 `mono`(tabular).
- **깊이는 그라디언트+소프트 섀도우+글래스로.** 카드/보더/그림자·큰 radius 남발 금지.
- **하단 탭 5개 고정**(오늘·기록·검색·습관·프로필). 6번째 금지.
- **의미색**(good/warn/danger)은 상태 전용, 브랜드 액센트와 섞지 않는다.

## 아키텍처
- **엔진(순수 함수) ↔ UI 분리.** 위젯은 엔진이 준 값을 렌더만. 로직 규칙은 격리 단위로 추가(`02 §5`).

## 검증
- 변경 후 `flutter analyze` 통과. UI 변경은 렌더로 계층·대비·정렬·밀도 확인.

## 진행 관리
- 마일스톤(문서·기능 완료) 시 `docs/00-overview.md`의 진행 상태를 갱신한다.
